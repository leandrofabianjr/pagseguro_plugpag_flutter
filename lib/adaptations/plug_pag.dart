class PlugPag {
  // Future<PlugPagAbortResult> abort();

  // void asyncAbort(PlugPagAbortListener listener);

  // void asyncAbortNFC(PlugPagAbortListener listener);

  // void asyncCalculateInstallments(String saleValue, PlugPagInstallmentsListener listener);

  // void asyncDeactivate(PlugPagActivationData activationData, PlugPagActivationListener listener);

  // void asyncGetLastApprovedTransaction(PlugPagLastTransactionListener listener);

  // void asyncIsAuthenticated(PlugPagIsActivatedListener isActivatedListener);

  // void asyncReadNFC(PlugPagNearFieldCardData cardData, PlugPagNFCListener listener);

  // void asyncReprintCustomerReceipt(PlugPagPrinterListener listener);

  // void asyncReprintEstablishmentReceipt(PlugPagPrinterListener listener);

  // void asyncSetStyles(PlugPagStyleData styleData, PlugPagSetStylesListener isSetStylesListener);

  // void asyncWriteNFC(PlugPagNearFieldCardData cardData, PlugPagNFCListener listener);

  // @Deprecated('Use asyncCalculateInstallments instead')
  // List<String> calculateInstallments(String saleValue);

  // List<PlugPagInstallment> calculateInstallments(String saleValue, int installmentType);

  // bool checkPermissionPlugPagService();

  // PlugPagInitializationResult deactivate(PlugPagActivationData activationData);

  // void disposeSubscriber();

  // void doAsyncApduCommand(List<int> command, int dataLength, PlugPagAPDUCmdExchangeListener listener);

  // void doAsyncInitializeAndActivatePinpad(PlugPagActivationData activationData, PlugPagActivationListener listener);

  // void doAsyncPayment(PlugPagPaymentData paymentData, PlugPagPaymentListener listener);

  // void doAsyncVoidPayment(PlugPagVoidData voidData, PlugPagPaymentListener listener);

  // PlugPagTransactionResult doEffectuatePreAuto(PlugPagEffectuatePreAutoData effectuatePreAutoData);

  // PlugPagTransactionResult doPayment(PlugPagPaymentData paymentData);

  // PlugPagTransactionResult doPreAutoCancel(String transactionId, String transactionCode);

  // PlugPagTransactionResult doPreAutoCreate(PlugPagPreAutoData plugPagPreAutoData);

  // PlugPagTransactionResult doPreAutoCreateWithKeyingData(PlugPagPreAutoKeyingData plugPagPreAutoKeyingData);

  // void doPrintAction(int action, String? phoneNumber);

  // PlugPagCardInfoResult getCardData();

  // PlugPagTransactionResult getKeyedInPreAutoData(PlugPagPreAutoQueryData plugPagPreAutoQueryData);

  // PlugPagTransactionResult getLastApprovedTransaction();

  // PlugPagPreAutoQueryResult getPreAutoList();

  // PlugPagSubAcquirerResult? getSubAcquirerData();

  // bool hasCapability(int capability);

  // bool hasSoftwareCapability(int operation);

  // bool hasSoftwareCapabilityWithMode(int operation, int mode);

  // bool hasSubAcquirer();

  // PlugPagPagBankActivationResult initPagBankActivation();

  // PlugPagInitializationResult initializeAndActivatePinpad(PlugPagActivationData activationData);

  // PlugPagInitializationResult initializeSubAcquirer(PlugPagSubAcquirerData data);

  // void invalidateAuthentication();

  // bool invalidateSubAcquirer();

  // bool isAuthenticated();

  // bool isServiceBusy();

  // PlugPagPrintResult reprintCustomerReceipt();

  // PlugPagPrintResult reprintStablishmentReceipt();

  // bool sendReceiptSMS(PlugPagReceiptSMSData smsData);

  // void setEventListener(PlugPagEventListener listener);

  // void setPlugPagCustomPrinterLayout(PlugPagCustomPrinterLayout plugPagCustomPrinterLayout);

  // PlugPagPrintActionResult setPrintActionListener(PlugPagPrintActionListener? printListener);

  // void setPrinterListener(PlugPagPrinterListener listener);

  // bool setStyleData(PlugPagStyleData styleData);

  // void startOnBoarding();

  // void unbindService();

  // int updateRemoteConfig();

  // PlugPagTransactionResult voidPayment(PlugPagVoidData voidData);
}
